export enum TradingSystem {
  USDT = `usdt`,
  BTC = `btc`,
  BOTH = `both`,
}

export enum BotStatus {
  CREATED = `created`,
  STOPPING = `stopping`,
  STOPPED = `stopped`,
  RUNNING = `running`,
}

export enum Exchange {
  STABLE = 'STABLE',
  BINANCE = 'BINANCE'
}
